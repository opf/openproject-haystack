# Temporary configuration to test local Ollama
# This overrides the default settings to use local Ollama instead of Docker

# Ollama Configuration - pointing to local instance
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=mistral:latest

# Keep other settings from .env.example
LOG_LEVEL=INFO
MODELS_TO_PULL=mistral:latest
REQUIRED_MODELS=mistral:latest
GENERATION_NUM_PREDICT=1000
GENERATION_TEMPERATURE=0.7
API_HOST=0.0.0.0
API_PORT=8000
